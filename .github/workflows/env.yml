name: Environment test

on: push

jobs:
  stage:
    runs-on: ubuntu-latest
    environment: 
      name: Stage
    steps:
      - run: echo Stage
  prod:
    runs-on: ubuntu-latest
    needs: stage
    environment:
      name: Production
    steps:
      - run: echo Prod